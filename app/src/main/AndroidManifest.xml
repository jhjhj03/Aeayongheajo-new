<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aeayongheajo"
        tools:targetApi="31">

        <!-- 메인 시작 액티비티 -->
        <activity android:name=".activity.AppStartActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MoodActivity" android:exported="false" />
        <activity android:name=".activity.GenreActivity" android:exported="false"/>
        <activity android:name=".activity.RecommendResultActivity" android:exported="false"/>
        <activity android:name=".activity.PlaylistNameActivity" android:exported="false"/>
        <activity android:name=".activity.MainActivity" android:exported="false"/>
        <activity android:name=".activity.PlaylistListActivity" android:exported="false"/>
        <activity android:name=".activity.PlaylistDetailActivity" android:exported="false"/>
        <activity android:name=".activity.LoadingActivity" android:exported="false"/>


    </application>

</manifest>